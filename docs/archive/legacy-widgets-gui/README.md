# Legacy Widgets GUI Archive

This folder stores the previous PySide6 Widgets desktop implementation for
historical reference only.

NexLog's active desktop interface is now the QML Cyber OS cockpit launched by:

```bash
python main_gui.py
```

Do not import these archived modules from runtime code or active tests.
